import { Meta, Stories } from '@storybook/addon-docs/blocks';
import * as AlertStories from './Alert.stories';

<Meta of={AlertStories} />

# Alert

An inline banner with six tones (`info`, `positive`, `warning`, `negative`, `critical`,
`neutral`), each pairing an icon with its color so meaning isn't carried by color alone.

## User story

**US-CW-023 — Component library — atoms & foundations**

As a clearline-web engineer, I want StatusBadge/MoneyDisplay/BudgetGauge and the 8 Tier-1 atoms
available as typed React components, so that I never hand-roll a button, badge, or money value
and risk missing an accessibility or formatting rule the design system already solved. Alert has
no acceptance criterion of its own in this story; it is governed by the tier's shared
constraints below.

## Acceptance criteria (shared across this tier)

- AC-01 — AC-04 — StatusBadge/MoneyDisplay/BudgetGauge, covered on their own docs pages.
- AC-05 — Checkbox wraps Radix `Checkbox` for correct `onCheckedChange`/`aria-checked` behavior.
- AC-06 — Button's `loading` state disables the button and blocks `onClick`.
- AC-07 — TextField's `state="error"` sets `aria-invalid` and `aria-describedby`.

Per this story's technical notes, every atom (Alert included) is styled with Tailwind utility
classes bound to the `--cl-*` tokens — no inline style objects, no CSS Modules.

<Stories />
