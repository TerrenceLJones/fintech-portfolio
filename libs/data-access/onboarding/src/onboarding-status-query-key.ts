/** Shared query key so every mutation that changes onboarding state can invalidate the same cache entry useOnboardingStatus reads. */
export const ONBOARDING_STATUS_QUERY_KEY = ['onboarding-status'];
